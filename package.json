{
  "name": "triptune-demo",
  "version": "0.1.0",
  "private": true,
  "description": "TripTune competition demo with a static frontend and lightweight API",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node --watch backend/server.js",
    "test": "node --test tests/*.test.js"
  }
}
